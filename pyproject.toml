[project]
name = "consumer-lending-risk-lakehouse"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.3",
    "psycopg2-binary>=2.9.12",
    "pyspark==3.5.3",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "delta-spark>=3.2,<3.3",
    "pytest>=9.1.1",
]
